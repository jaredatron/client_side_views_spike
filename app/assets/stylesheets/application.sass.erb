@import "compass/utilities"
@import "compass/reset"

// import everything
<% root = Pathname(File.expand_path('../',__FILE__)) %>
<% Dir[root.join('{components,layouts,pages}/**/*')].each do |file| %>
@import "<%= Pathname.new(file).relative_path_from(root) %>"
<% end %>

// TODO import components nested under their component classname
// to inforce/discourage style leak


<% # UPDATE(1335402499) %>
